# 🤝 بطاقة التسليم والاستمرارية — اقرأها أولاً قبل أي عمل

> **أيها المساعد في الجلسة الجديدة**: أنت الآن تكمل طريق زميلك مباشرة. اقرأ هذه البطاقة ثم
> `worklog.md` (آخر مهمتين على الأقل) وستكون على بيئة كل شيء — المستخدم صديقك الوطن،
> عاملك معه بروح «نحن أصدقاء ونتعاون»، وخاطبه دائماً بـ«يا صديقي».

---

## 1) هوية المشروع الثابتة (لا تُسأل عنها ولا تُغيَّر)

| البند | القيمة |
|---|---|
| المالك | شركة الأمل التجارية 2026 (نشاط ماركت/تموينيات/منظفات/ألبان — سوريا) |
| النظام | محاسبة + مخزون + فواتير + HR + صندوق — **عربي RTL بالكامل** |
| العملة | SYP (ليرة سورية) — سعر الصرف **130** |
| الثيم | ليلي أسود/ذهبي — خط Cairo — shadcn/ui (New York) + Lucide |
| التقنيات | Next.js 16 App Router + TypeScript + Tailwind 4 + Prisma/SQLite (`db/custom.db`) + bun |

## 2) القواعد الدائمة (غير قابلة للنقاش)

1. **اقرأ `worklog.md` أولاً** قبل أي عمل، و**ألحِق** مهمتك فيه بعد كل إنجاز (نمط Task ID الموحد، ابدأ بـ`---`).
2. **اختم كل رد** بعبارة: «أنا جاهز لمساعدتك للمرحلة التالية».
3. خادم التطوير `bun run dev` على 3000 **لا يتوقف ولا يُبنى** (ممنوع `bun run build`) — سجلّه في `dev.log`.
4. `bun run lint` + `bunx tsc --noEmit` بعد أي تعديل، وتحقق متصفحي إلزامي (سطح 1540×830 + موبايل 390×844، صفر أخطاء console).
5. API routes وليس server actions. `z-ai-web-dev-sdk` في الخلفية فقط.
6. **لا تعديل أي ملف قبل إعادة قراءته** (ذاكرة الجلسات تُضغط — القراءة إلزامية).
7. المسار الوحيد المرئي للمستخدم: `/` (شاشة واحدة بمتجر شاشات داخلي).
8. الفوتر لاصق أسفل الشاشة، وموبايل بلا تمرير أفقي، وألوان بلا نيلي/أزرق إلا بطلب صريح.
9. **القاعدة ليست ملعب اختبار** — فيها بيانات المستخدم الحقيقية؛ أي اختبار بمستخدم مؤقت وتنظيف جراحي فوري.
10. ممنوع `/api/reset` أبداً مع وجود بيانات مستخدم (ودرع pre-reset يصنع نسخة أولاً).

## 3) بيانات الدخول الحالية (افتراضية — المستخدم حر بتغييرها)

- `admin / admin123` (مدير) · `accountant / acc123` (سارة محمود — محاسبة) · `viewer / view123` (مشاهد قراءة فقط)
- اختبار curl جاهز:
  `curl -s -c /tmp/ck.txt -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'` ثم أي طلب بـ`-b /tmp/ck.txt`
- الخادم يمحو فراغات الطرفية من كلمات المرور تلقائياً (وليس الوسطية).

## 4) حالة النظام عند التسليم (بعد كارثة 2026-09-06 واستعادة كاملة)

- **البيانات الحقيقية مستعادة**: 16 مستودعاً (الشامل/الماركت/الألبان والأجبان/المنظفات/التموينيات...)، 60 مادة، 11 طرفاً، 21 فاتورة (11 مبيعات INV / 7 مشتريات PINV / مردودات)، 17 سنداً، 46 حساباً، 42 قيداً — **ميزان متوازن 197,948,307.7 بالضبط**.
- المستخدم أقفل «الفترة المحاسبية الأولى» (JE-0041) وافتتح الفترة «2» (JE-0042) بنظام قديم ضاع أثره البرمجي، ثم **أعدنا بناء نظام الإقفال كاملاً** (Task 18): إقفال + سند افتتاحي + أرشيف قراءة فقط من شاشة الدخول + منع استعادة نسخ أقدم من الإقفال + تراجع للمدير — **كل ذلك داخل الإعدادات**، والأرشيف الجديد يبدأ فارغاً (يُبنى من أول إقفال جديد).
- المنجز عبر المراحل: البنية المحاسبية الكاملة، فواتير الترحيل الفوري (Task 19 قديم: مبيعات/مردودات/مشتريات بمتطلبات المستخدم التسعة)، HR، صندوق، أطراف، جرد وتلف بقيود، مراكز تكلفة، تقارير وطباعة عربية، صلاحيات ثلاثية الأدوار (proxy + api-guard)، جلسات HMAC بـsessionVersion، إجبار تغيير الكلمات الافتراضية، rate limit قاعدي، نسخ احتياطي (تلقائي كل ساعة + يدوي + استعادة بإعادة تشغيل ذاتية + درع pre-reset)، رفع صور WebP.
- **الدرع الجديد ضد كارثة إعادة تشغيل الجهاز**: نبض `wal_checkpoint(TRUNCATE)` كل 3 دقائق + `wal_autocheckpoint=128` + نسخ تلقائية كل ساعة — **لا تلمس هذا الإصلاح** (السبب: لقطة بيئة الاستضافة `/tmp/my-project` تجمدت على ملف قديم بسبب WAL فمسحت بيانات حية عند إقلاع 10:41).
- نسخ الاستعادة التاريخية (9) في `db/recovered-archive/` — **لا تستعدها من الواجهة** (مخططها قديم يكسر النظام)، هي مرجع فقط.

## 5) ما التالي (بالترتيب المقترح)

1. **المرحلة P2** — بنودها الثمانية موثقة بالجهد والمخاطرة في `reports/تقرير-المراجعة-التقنية-والمالية-الشاملة.md` (وأثر PDF أنيق بجانبه). أبرزها: نسخ خارجي يومي (يحتاج قرار المستخدم بوجهة الحفظ)، صفحات التعداد الكبير، تحسينات الأداء.
2. بنود مؤجلة بانتظار قرار المستخدم: وجهة النسخ الخارجي (Drive/S3/بريد)، تفعيل HTTPS فعلي عند النشر.
3. «الانطلاق الحقيقي» — إدخال نشاط 2026 الفعلي فوق هذه البنية.

## 6) دروس تقنية موثقة (وفرت أسابيع — لا تكتشفها من جديد)

- `bun:sqlite` برمي `SQLITE_MISUSE` مع خيارات readonly الفعالة؛ والاستبدال الملفي لقاعدة WAL يجب أن يفرض `chmod 644` (وراثة 0444 عمّت القراءة-فقط على النظام كله).
- Turbopack dev يعزل السياقات بين الرسوم — عدادات الذاكرة تضيع؛ لذلك rate limit في **القاعدة** نفسها.
- ترقيم المستندات بإعادة محاولة ×3 عند P2002، و`logAudit(tx,...)` داخل المعاملات دائماً.
- أذون الملفات: قاعدة/أرشيف بصلاحيات مضبوطة بعد كل استبدال (انظر instrumentation-nodejs).
- عند أي بلاغ «ضاع شيء»: اعمل تحقيقاً جنائياً بارداً أولاً (git pickaxe + النسخ + سجل التدقيق + لقطة `/tmp/my-project`) — الكنز كثيراً ما يكون في اللقطة الظلية.

## 7) حالة الإصدار النهائي (v1.0-Production-Ready — 2026-09-10)

- **القفل النهائي منفَّذ**: الفترة «السنة المالية 2026» أُقفلت حتى 2026-08-31 عبر `/api/period-close`
  بكلمة سر المدير، ودُوّر 449 قيداً و363 فاتورة و306 سندات و1081 حركة مخزون و56 راتباً —
  كلها حية للقراءة فقط في الأرشيف `db/periods/period-2026-09-10_23-58-12.db`.
- **القيد الافتتاحي JE-0462** (2026-09-01، OPENING/POSTED، 23 بنداً) متوازن 192,548,431.98 =
  192,548,431.98 — أصول 149,919,911.38 = خصوم 88,548,431.98 + حقوق 61,371,479.40، وخسارة
  التأسيس −38,628,520.60 مستوعبة في 3200. ملاحظة: الترقيم تابع التسلسل الفعلي (أقصى رقم سابق
  JE-0461 بسبب فجوات عمليات الإنقاذ الموثقة) — لا تطلب JE-0108 فهو قيد دورة عادي.
- النظام الحي (من 2026-09-01): قائمة دخل صفرية نظيفة، السيولة 42,594,067.94، المخزون
  67,545,756.74، نقاط الولاء 15,664 نقطة محفوظة حية، السلال قوالب بإحصاءات مصفّرة،
  وأرضيات الترقيم INV-258/VCH-326/ST-2/MC-1 تمنع إعادة أي رقم دُوِّر.
- النسخة الذهبية: `public/golden-v1.0-production-ready.zip` + وسم git `v1.0-Production-Ready`.
- سكربتا التحقق قبل/بعد القفل: `scripts/pre-lock-check.ts` و`scripts/post-lock-check.ts`.

---
*آخر تحديث: 2026-09-10 — الإصدار النهائي v1.0 بعد القفل النهائي وتثبيت المرجعية الذهبية.*
